import React from 'react';
import UseParamsReference from '!!raw-loader!../../reference/use-params.md';
import ReferenceDisplay from '../../components/ReferenceDisplay';

const UseParams = props => {
  return (
    <ReferenceDisplay source={UseParamsReference} />
  );
};

UseParams.propTypes = {};

export default UseParams;
